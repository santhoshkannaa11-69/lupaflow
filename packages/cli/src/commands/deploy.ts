import chalk from "chalk"

export async function deployCommand(): Promise<void> {
  console.log(chalk.cyan("\n  LupaFlow Deploy\n"))
  console.log(chalk.yellow("  Cloud deployment coming in a future release.\n"))
}
